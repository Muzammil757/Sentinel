# Direct dependencies only -- pip resolves each package's own transitive
# requirements at install time, so those are not pinned here individually.
fastapi==0.141.1
uvicorn==0.52.4
PyYAML==6.0.3
jsonschema==4.26.0
pytest
supabase==2.31.0
python-dotenv==1.2.3
httpx==0.28.1
