The container. Hairline border, 6px radius, no shadow. `tone="console"` for the dark execution/audit surfaces.

```jsx
<Panel label="GOVERN" title="Decision" actions={<Button size="sm">Audit trail</Button>}>…</Panel>
```

Set `padded={false}` when the body is a row list that should bleed to the border.
