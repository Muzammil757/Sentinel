Multi-line field — in Sentinel this is almost always a reviewer annotation, never an override instruction.

```jsx
<Textarea label="Review note" rows={4} counter="82/500" hint="Annotations are appended to the audit trail." />
```
