Small classifier for counts, codes and categories. For governance outcomes use `StatusBadge` instead.

```jsx
<Badge tone="accent" mono>P1</Badge>
<Badge tone="conflict">2 agents disagree</Badge>
```
