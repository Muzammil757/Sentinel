Monochrome glyph that inherits `currentColor`; use it for every icon in Sentinel instead of inline SVG or emoji.

```jsx
<Icon name="shield-check" size={16} />
```

Set of record is Lucide (substituted — no brand icon set was supplied). Icons never carry color on their own: color comes from the surrounding text or status token.
