Square glyph-only control for toolbars, row affordances and panel headers.

```jsx
<IconButton icon="panel-right-close" label="Collapse inspector" />
<IconButton icon="filter" variant="outline" active />
```

Always pass `label`. Use `ghost` inside dense toolbars, `outline` when it sits alone on a surface.
